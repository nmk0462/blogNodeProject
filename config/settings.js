module.exports = {

    // transformer param key
    transformer: {
        paramKey: 'includes'
    },

    // pagination settings
    pagination: {
        limit: {
            default: 20,
            max: 100
        }
    },

    // database notifications
    database_notifications: {},

    resetPassword: {
        expiryTime: 24
    },

    // ...

};
